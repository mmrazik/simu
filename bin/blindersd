#!/usr/bin/env python
import os
import sys

BASEDIR = os.path.dirname(__file__)
if os.path.isdir(os.path.join(BASEDIR, '../simu')):
    sys.path.insert(0, os.path.join(BASEDIR, '..'))

from simu.blindersd import check_status

if __name__ == "__main__":           
    check_status()
